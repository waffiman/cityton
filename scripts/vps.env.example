# Copy to scripts/vps.env and adjust if needed (vps.env is gitignored).
VPS_HOST=82.165.243.89
VPS_USER=root
VPS_PORT=22
# Dedicated key used by scripts/vps.sh (generated locally, not committed).
VPS_KEY="$HOME/.ssh/cityton_vps"
